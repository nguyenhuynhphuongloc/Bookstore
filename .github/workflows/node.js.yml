name: Frontend CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-frontend:
    runs-on: ubuntu-latest

    strategy:
      matrix:
<<<<<<< HEAD
        node-version: [18.x] # Bạn có thể thêm 20.x nếu muốn test thêm version

    steps:
      # 1️⃣ Clone code từ repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Cài đúng Node.js version và bật cache npm
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json  # 💡 CHỈ RÕ FILE LOCK TRONG FRONTEND

      # 3️⃣ Cài dependencies trong thư mục frontend
      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      # 4️⃣ Build project (nếu có script build)
      - name: Build frontend
        working-directory: frontend
        run: npm run build --if-present

      # 5️⃣ Chạy test (nếu có script test)
      - name: Run tests
        working-directory: frontend
        run: npm test
=======
        node-version: [14.x, 16.x, 18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm install
    - run: npm run build --if-present
    - run: npm test
>>>>>>> 86a19c8a543d5d11abd8c0b6f419eadf7142edac
